{
  "name": "rapier-pages-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:pages": "npm run build && rm -rf docs && cp -r dist docs && touch docs/.nojekyll"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "three": "^0.181.1"
  },
  "devDependencies": {
    "vite": "^7.1.10"
  }
}
